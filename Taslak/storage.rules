rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /uploads/{allPaths=**} {
      // Anyone can read uploads (images)
      allow read: if true;
      // Only logged-in users (or admins) can upload files
      allow write: if request.auth != null;
    }
  }
}
